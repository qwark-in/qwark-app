name: Create Preview Builds

on:
  push:
    branches: ["master"]

  tags: ["v1-preview"]

  jobs:
    build_android_preview:
      name: Build Anroid Preview
      type: build
      params:
        platform: android
        profile: preview
